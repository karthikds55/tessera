"""Streamlit fundamentals screener for tessera.

:mod:`dashboard.queries` holds the typed, Streamlit-free SQL layer over the
``mart_fundamentals_annual`` / ``dim_company`` marts; :mod:`dashboard.app` is the
thin Streamlit UI on top. Launch with ``inv dashboard``.
"""

from __future__ import annotations

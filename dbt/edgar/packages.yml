packages:
  # Cross-database test/macro helpers (surrogate keys, accepted_range,
  # unique_combination_of_columns) the marts rely on.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]
  # Data-observability suite; installed now, models kept disabled in
  # dbt_project.yml until a dedicated observability PR wires it up.
  - package: elementary-data/elementary
    version: [">=0.16.0", "<0.18.0"]
